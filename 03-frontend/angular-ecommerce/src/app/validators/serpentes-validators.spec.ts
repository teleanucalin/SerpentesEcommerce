import { SerpentesValidators } from './serpentes-validators';

describe('SerpentesValidators', () => {
  it('should create an instance', () => {
    expect(new SerpentesValidators()).toBeTruthy();
  });
});
